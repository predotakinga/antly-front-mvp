import { styled } from "@mui/system";
import { Link } from "react-router-dom";

import { Box } from "@mui/material";

export const WrapperNoMargin = styled(Box)({
  display: "flex",
  justifyContent: "center",
  verticalAlign: "middle",
});

export const WrapperPlaces = styled(Box)({
  display: "flex",
  justifyContent: "center",
  verticalAlign: "middle",
  marginTop: "15px",
});

export const WrapperTitle = styled(Box)({
  display: "flex",
  justifyContent: "center",
  verticalAlign: "middle",
  marginTop: "20px",
});

export const WrapperProps = styled(Box)({
  display: "flex",
  justifyContent: "center",
  verticalAlign: "middle",
  marginTop: "5px",
  color: "#AB464F",
});
